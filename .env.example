# Webhook Hub Environment Configuration

# Required: Discord webhook URL for sending notifications
# Get this from Discord Server Settings > Integrations > Webhooks
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Optional: Vercel webhook secret for signature verification
# Set this in your Vercel project webhook settings for security
VERCEL_WEBHOOK_SECRET=your_vercel_webhook_secret_here

# Optional: GitHub webhook secret for signature verification
# Set this in your GitHub repository webhook settings for security
GITHUB_WEBHOOK_SECRET=your_github_webhook_secret_here

# Optional: Server configuration
PORT=3000
NODE_ENV=production

# Optional: CORS configuration (comma-separated origins)
# Leave empty or set to * to allow all origins
ALLOWED_ORIGINS=https://yourdomain.com,https://anotherdomain.com
